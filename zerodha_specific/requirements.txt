# Zerodha Trade Copier — Dependencies
# Lightweight: no PostgreSQL, no Redis, no Selenium

# FastAPI & server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (SQLite — async)
sqlalchemy[asyncio]==2.0.23
aiosqlite==0.19.0

# HTTP client (async)
httpx==0.25.2

# Zerodha Kite Connect SDK (for generate_session only)
kiteconnect==4.2.0

# TOTP for automated login
pyotp==2.9.0

# Encryption for stored credentials
cryptography==41.0.7

# Timezone handling
pytz==2023.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
